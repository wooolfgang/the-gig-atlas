type Mutation {
  deleteUser(id: ID!): Boolean!
}

interface User {
  id: ID!
  name: String
  email: String
  asEmployer: Employer
  role: UserRole
  createdAt: Date
}

enum UserRole {
  ADMIN
  MEMBER
}
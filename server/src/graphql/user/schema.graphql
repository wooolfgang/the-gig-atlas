type Query {
  user: User!
}

type Mutation {
  deleteUser(id: ID!): Boolean!
}

type User {
  id: ID
  name: String
  email: String
  asEmployer: Employer
  role: UserRole
  createdAt: Date
}

enum UserRole {
  ADMIN
  MEMBER
}

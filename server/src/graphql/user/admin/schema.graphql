type Query {
  admin(id: ID!): Admin
  admins: [Admin]!
}

type Mutation {
  deleteAdmin(id: ID!): Boolean,
}

type Admin implements User {
  id: ID
  name: String!
  email: String!
  createdAt: Date
}

input AdminInput {
  name: String!
  email: String!
  password: String!
}
